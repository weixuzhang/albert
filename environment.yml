name: albert-sandbox
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
    - openai>=1.3.0
    - pytest>=7.0.0
    - pyyaml>=6.0


# Environment variables (optional)
variables:
  # Set these in your conda environment or .env file
  OPENAI_API_KEY: ""